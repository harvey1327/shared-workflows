name: dynamic-workflow-2

on:
  workflow_call:

jobs:
  build:
    name: Build & Test
    runs-on: ubuntu-latest
    steps:
      - name: Log
        run: echo "dynamic-workflow-2"
      - name: Checkout code
        uses: actions/checkout@v6
      - name: Run build script
        run: ./build.sh
