name: shared-ci

on:
  workflow_call:

jobs:
  rollout:
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.generate.outputs.result }}
    steps:
      - name: generate
        id: generate
        run: echo "result=1" >> $GITHUB_OUTPUT

  dw1:
    needs: rollout
    if: ${{ needs.rollout.outputs.version == 1 }}
    uses: harvey1327/shared-workflows/.github/workflows/dynamic-workflow.yml@v1
  
  dw2:
    needs: rollout
    if: ${{ needs.rollout.outputs.version == 2 }}
    uses: harvey1327/shared-workflows/.github/workflows/dynamic-workflow.yml@v2
